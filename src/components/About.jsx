import { Sidebar } from "lucide-react";
import SRank from "./SRank";

const About = () => {
    return (
        <div className="flex justify-between">
            <SRank />
        </div>
    );
};

export default About;